import mongoose from "mongoose";

const accountabilitySlipSchema =  new mongoose.Schema({

})


const accountablitySlip =  mongoose.model('Accountability',accountabilitySlipSchema);

export default accountablitySlip